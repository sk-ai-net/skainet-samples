package sk.ainet.samples.kernelrace

/** Desktop/wasm display version — keep in sync with `skainet` in gradle/libs.versions.toml.
 *  Android reads the real catalog value at build time via `BuildConfig.SKAINET_VERSION` instead. */
const val SKAINET_VERSION = "0.39.1"
